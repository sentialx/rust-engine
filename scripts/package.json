{
  "name": "graviton-baseline-generator",
  "version": "1.0.0",
  "description": "Generate SVG baselines from Chromium for layout comparison tests",
  "scripts": {
    "generate": "npx ts-node generate_baselines.ts"
  },
  "dependencies": {
    "playwright": "^1.40.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.0"
  }
}
